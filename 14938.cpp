#include <bits/stdc++.h>
using namespace std;

int item[101];
int visit[101];
int dis[101][101];
int n, m, r, l;
int start, finish;

int main() {
	cin >> n >> m >> r;
	for (int i = 1; i <= n; i++)
		cin >> item[i];

	for (int i = 0; i <= r; i++) {
		cin >> start >> finish >> l;
		dis 
	}

}